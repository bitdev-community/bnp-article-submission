---
description: A component that renders a heading via Typography
labels: ["Typography", "Heading"]
---

import { Heading } from "./heading";

A component that renders a heading. Use with a `ThemeProvider to enforce styling.

### Component usage

```js
<Heading value="Testing Typography" />
```

### Render the heading.

```js live
<Heading value="Testing Typography" />
```
