---
description: 'A function that accepts theme properties and creates a usable theme object'
labels: ['utility', 'theme utility']
---

API:

```ts
function themeCreator(lightTheme() || darkTheme()): Theme;
```
