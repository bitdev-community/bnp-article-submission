---
description: A component rendering a combination of an icon + text
labels: ['rich ui', 'icon and text']
---

import { TextIcon } from './text-icon';

A component that renders an icon + text.
Use with a `ThemeProvider` to enforce MUI Themeing.

### Component usage

```js
<TextIcon
  icon={</></>}
  text="Bit.dev"
  fontSize={16}
  fontWeight={500}
/>
```

### Rendering sample text icon

```js live
<TextIcon
  icon={</></>}
  text="Bit.dev"
  fontSize={16}
  fontWeight={500}
/>
```
